import { DatePickerFormatDirective } from './date-picker-format.directive';

describe('DatePickerFormatDirective', () => {
  let directive: DatePickerFormatDirective;
  it('should create an instance', () => {
    expect(directive).toBeDefined();
  });
});
